import React from 'react';
import { access } from './acces';
import { onHold } from './onHold';
import { userPortal } from './userPortal';

export function Login({email, access, updateAccess}) {
    return (
        <main className="py-3 text-center">
                
        </main>
    );
}